describe('Screeps Test Server (with mods)', () => {
  let server;

  afterEach(async () => {
    server.stop();
  });

  it.todo('should modify mods.json with given mod paths');

  it.todo('should launch test server with specified mod enabled');
});
